import styled from 'styled-components'
import { Form as Unform } from '@unform/web'

const Form = styled(Unform)`
    display: grid;
    grid-gap: 2rem;
    align-items: center;
`

export default Form
